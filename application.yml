---
- name: Install application
  hosts: all
  become: true

  tasks:
    - name: Install Apache
      ansible.builtin.dnf:
        name: httpd
        state: present

    - name: Start Apache
      ansible.builtin.systemd:
        name: httpd
        state: started
        enabled: true



